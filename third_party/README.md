# Third-Party Dependencies

All vendored external source dependencies live under this directory.

These trees are intentionally kept separate from product-owned code. Vendored does not mean internal.

Current planned dependencies:

- `juce/`
- `hise/`

Every vendored dependency should include or be paired with provenance information recording its upstream origin and any local modifications.

See `vendor-manifest.md` for the currently imported snapshots.
