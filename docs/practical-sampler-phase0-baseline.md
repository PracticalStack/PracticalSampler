# Practical Sampler identity initiative — Phase 0 baseline

Date: August 15, 2026  
Status: Complete — Gate G0 passed  
Source commit: `2cfa6ae462bd581980addd243384172db8f81d05`

## Approved presentation identity

| Field | Current value | Approved value |
|---|---|---|
| Plug-in / product name | Decent Rhapsody Studio | Practical Sampler |
| Company / vendor name | Decent Rhapsody | Practical Sampler Project |
| Proposed repository name | DecentRhapsodyStudio | PracticalSampler |

This is an identity-layer change. It does not authorize a logo, image, visual-branding, internal
component, native-format, schema, namespace, target, plug-in-code, class-ID, parameter-ID, or bundle-ID
rename. Backward compatibility with REAPER projects created under the old display name is not a gate.

## Occurrence ledger

The workspace-wide audit is generated by
`tools/audit-practical-sampler-identity.ps1` using the ordered rules in
`tools/practical-sampler-identity-rules.json`. The generated repository result is
`docs/practical-sampler-phase0-identity-ledger.json`.

The August 15 baseline contains 552 exact presentation-string occurrences and zero unclassified
occurrences:

| Classification | Count | Phase treatment |
|---|---:|---|
| CHANGE | 301 | Replace active product or company presentation in the applicable delivery phase. |
| REGENERATE | 69 | Recreate active REAPER fixtures or other derived output from renamed sources. |
| PRESERVE-HISTORICAL | 56 | Keep dated evidence truthful to the build that produced it. |
| DEFER | 126 | Keep decision records, external REAPER projects, and unapproved partial-name copy unchanged. |

`PRESERVE-TECHNICAL` has no exact spaced presentation-string hit in this scan. Technical identity is
captured and guarded separately because those identifiers use compact `drs`, `DRS`, hexadecimal,
bundle-ID, or schema forms.

Run the gate from the repository root:

```powershell
./tools/audit-practical-sampler-identity.ps1
```

The script exits nonzero if a new old-name occurrence does not match an approved rule. Rules are
intentionally path- and field-specific; adding a broad fallback rule is not an acceptable way to
close an audit failure.

## Source and derived-copy relationships

| Source / owner | Derived or external copy | Phase 0 disposition |
|---|---|---|
| Current workspace HTML plans and mockups | `DecentRhapsodyStudio/DRSWeb/protected-content/reference` | Update the owning source first, then regenerate or synchronize the protected reference copy. |
| Runtime/build configuration and current documentation | `build/`, VST3 bundles, installer staging, generated caches | Never hand-edit; rebuild or regenerate after source presentation changes. |
| Current host-validation sources and scripts | `DecentRhapsodyStudio/validation/reaper` active `.rpp`, track chunks, and scanner caches | Regenerate for Practical Sampler; old-name recall is not required. |
| User-owned workspace `ReaperProjects` | None | Defer; do not rewrite user projects. |
| Dated reports, logs, backups, and engineering evidence | None | Preserve as historical evidence and keep it out of current guidance. |

## Stable technical identity

The machine-readable baseline is
`docs/practical-sampler-technical-identity-baseline.json`. The verification command is:

```powershell
./tools/verify-practical-sampler-technical-identity.ps1
```

It passed against source and the built Debug VST3 metadata. The frozen values include:

| Contract | Baseline |
|---|---|
| CMake project / app / plug-in / compatibility targets | `DecentRhapsodyStudio`, `DecentRhapsodyStudioApp`, `drs_plugin_bundle`, `DecentRhapsodyStudioPlugin` |
| Application / plug-in bundle IDs | `com.decentrhapsody.studio.app`, `com.decentrhapsody.studio.plugin` |
| Manufacturer / plug-in codes | `Dcrh` (`0x44637268`), `Drs0` (`0x44727330`) |
| VST3 component CID | `ABCDEF019182FAEB4463726844727330` |
| VST3 controller CID | `ABCDEF011234ABCD4463726844727330` |
| Native extensions | `.drsproj`, `.drinst`, `.drstrm`, `.drpkg` |
| Package magic | `DRSPKG1\0` / `44 52 53 50 4B 47 31 00` |
| Schema contracts | Nine named schema/version pairs in the JSON baseline |
| Host parameters | 16 stable `macro.*` parameter IDs |

The verifier discovers `moduleinfo.json` below the canonical VST3 artifact directory, so it continues
to verify the stable CIDs after the visible bundle filename changes in Phase 1.

## Pre-change build and qualification evidence

The complete Debug build succeeded with 410 build actions:

```powershell
cmake --build --preset build-debug -j 2
```

Focused identity-sensitive qualification is green:

| Test | Result | Coverage |
|---|---|---|
| `drs.phase0.smoke` | PASS, 6.48 s | Loads the app/core seams and scans the compiled VST3 through JUCE's VST3 host layer. |
| `drs.host_state.vst3_qualification` | PASS, 2.45 s | Instantiates the built VST3 and proves host-state/audio qualification. |
| `drs.phase3.round_robin_hardening` | PASS individually, 11.04 s | Diagnostic control for a broader-suite runner stall. |
| `drs.sprint6.integration_hardening` | PASS individually, 10.11 s | Diagnostic control for the same runner stall. |
| `drs.phase1.draft_playback_facade` | PASS individually, 6.94 s | Diagnostic control for a sequential aggregate-run stall. |

On this Windows execution harness, aggregate CTest runs stalled while otherwise-green executables
were combined, first with `-j 2` and later at the fifth test with `-j 1`. Each stalled test passed
immediately when selected alone in a fresh terminal. The aggregate process was stopped after the
diagnosis; no test failure was emitted. This is recorded as a runner limitation, not as full-suite
green evidence. The full-suite gate belongs to Phase 3, where it must be rerun in a stable CI or
local runner.

## Pre-change artifact evidence

| Artifact | Visible metadata | SHA-256 |
|---|---|---|
| `Decent Rhapsody Studio.exe` | Product / description: Decent Rhapsody Studio; company: Decent Rhapsody; version: 0.1.0 | `BF8DD19FDE8CAE66E19B4C2689C5A2AAEA609490FF9F09E26CBC637AA4511442` |
| `Decent Rhapsody Studio.vst3/Contents/x86_64-win/Decent Rhapsody Studio.vst3` | Product / description: Decent Rhapsody Studio; company: Decent Rhapsody; version: 0.1.0 | `4D9F8E099109690AD7EB6F4E22CFD88D4650EE82883197AD8C220448407B5829` |
| VST3 `moduleinfo.json` | Name: Decent Rhapsody Studio; vendor: Decent Rhapsody; both frozen CIDs present | `0F49685645D0F33CB9DE28EBFFB5F1954065B2C89953EE57E53761C81C32CCB0` |

These are evidence of the pre-change presentation identity, not values to preserve. Phase 1 should
change the visible names and artifact filenames while the technical-identity verifier remains green.

## Gate G0 decision

Gate G0 passes because the identity decision is explicit, every scanned old presentation-string
occurrence is classified, the technical and serialized identity is captured in machine-readable
form, the complete Debug build succeeds, and focused VST3 scan/state qualification is green. No
runtime product or company presentation string was changed in Phase 0.
